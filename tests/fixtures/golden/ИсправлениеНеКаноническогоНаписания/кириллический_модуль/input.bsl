&насерверебезконтекста
процедура Проверить(знач Параметр) экспорт
	если Параметр = null или Параметр = неопределено тогда
		возврат истина;
	иначеесли не Параметр тогда
		вызватьисключение "Ошибка";
	иначе
		возврат ложь;
	конецесли;
конецпроцедуры

// если null возврат
Текст = "если null
|возврат ложь
|";
ИмяЕсли = Истина;
